# OS
.DS_Store
**/.DS_Store
Thumbs.db

# Raw video source files (use converted .mp4 instead)
*.mov
*.avi
*.mkv

# Editor
.vscode/
*.swp
*.swo

# Env
.env
.env.local
*.env

# Fleet Watch — live AIS snapshot, generated on the VPS by
# scripts/fleet-relay/relay.py every ~5s. Never commit generated data.
assets/data/fleet-live.json
assets/data/fleet-live.json.tmp

# Maritime news — generated on the VPS by scripts/fetch-maritime-news.py
# via cron every ~20-30 min. Never commit generated data.
assets/data/maritime-news.json
assets/data/maritime-news.json.tmp

# Python
__pycache__/
*.pyc
